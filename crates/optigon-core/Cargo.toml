[package]
name = "optigon-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Optigon core — domain abstraction, packaged implementations, and the regret-scored chooser (candle training + inference). Language-agnostic; the napi/pyo3 bindings are thin wrappers over this."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
candle-core = { workspace = true }
candle-nn = { workspace = true }
