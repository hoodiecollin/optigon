# optigon (Python)

The Python native extension for [Optigon](../../README.md) — a thin PyO3 wrapper
over `optigon-core`. Build locally with `maturin develop --release` (or
`make py-build` from the repo root), then `import optigon`.
